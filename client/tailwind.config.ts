import scrollbar from 'tailwind-scrollbar';

export default {
  theme: {
    extend: {
      fontFamily: {
        jost: ['Jost', 'sans-serif'],
      },
    },
  },
  plugins: [
    scrollbar,
  ],
};